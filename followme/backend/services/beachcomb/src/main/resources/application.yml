properties:
  matching:
    matching-distance-tolerance-km: 0.2
    matching-time-tolerance-ms: 1000
    following-vehicle-speed-discrepancy-kmh: 5
    following-vehicle-time-discrepancy-ms: 5000

spring:
  application:
    name: Beachcomb Service
  data:
    mongodb:
      uri: mongodb+srv://e12007552:2bXPgfoEjWReqZvX@ClusterDSE2024.lo0qujn.mongodb.net/?retryWrites=true&w=majority&appName=ClusterDSE2024
      database: beachcomb
  cloud:
    gcp:
      project-id: dse2024-425312
      credentials:  #credential.location can be removed in deployment as Google cloud will get this from the environment itself
        location: classpath:pubsub-key.json
      pubsub:
        health:
          executor-threads: 1

pubsub:
  topic: followme_matching
  subscription: followme_matching-sub

logging:
  level:
    root: info

server:
  port: 8002